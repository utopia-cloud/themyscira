spring:
  jpa:
    hibernate:
      ddl-auto: update
    database: MYSQL
  datasource:
    url: jdbc:mysql://localhost:3306/themyscira
    username: springuser
    password: ThePassword
  batch:
    initialize-schema: always